id: 250
code: null
title: >-
  Retirer le kind « quick » — le type de colonne + size suffisent, le split quick/task est du bruit
  (décision Rémi)
status: done
tags:
  - engineering
  - model
  - simplification
size: M
detail: >-
  Décision Rémi : depuis les colonnes par TYPE, le couple quick/task ne porte plus qu'un niveau de
  cérémonie redondant avec `size` (S/M/L) et impose une micro-décision à chaque création
  (bikeshedding). On le supprime. `kind` devient `task | milestone` (on GARDE milestone : il
  verrouille des tâches via dependsOn, sémantique distincte). Chantier : (1) tasks.ts : kind sans
  'quick', défaut 'task'. (2) validate.ts : rejeter kind 'quick' sur les actives (l'archive garde le
  sien, non re-validée). (3) Migration : les quick existants → task (script jetable, git mv pas
  nécessaire, juste réécrire kind ; ou omettre kind=task par défaut). (4) taskWrites/CLI : la
  commande `quick` devient un alias de `add` (rétrocompat) ou disparaît avec warning ; retirer
  l'exception « --verification optionnel pour quick » → verification ENCOURAGÉE mais non bloquante
  pour tous (préserver la légèreté du done trivial sans un kind dédié). (5) UI : retirer la zone «
  Mini » du Backlog (les ex-quick rejoignent la liste To-do triée par température) + le glyphe quick
  + le one-click-done de la zone Mini (à préserver comme affordance de done sur toute tâche si on y
  tient). (6) Skill/docs : retirer quick partout, l'échelle de décision devient type + size. Voisin
  : c'est un mini-jalons (kind au lieu de stage+team) mais bien plus petit.
refs:
  - src/lib/tasks.ts
  - src/lib/validate.ts
  - src/lib/taskWrites.ts
  - src/components/TaskColumns.tsx
  - src/components/Backlog.tsx
  - scripts/task.mjs
  - skills/roadmapped/SKILL.md
links: []
dependsOn: []
epic: null
source: ai
createdAt: "2026-07-10T10:36:56"
startedAt: "2026-07-10T11:19:40"
updatedAt: "2026-07-18T21:29:43"
completedAt: "2026-07-10"
commit: a059ad2
outcome: >-
  Kind « quick » supprimé (décision Rémi) : kind = task|milestone (milestone gardé). validate
  rejette kind:quick actif (archive intacte). 54 quicks migrés → task. La commande/outil `quick`
  devient un ALIAS de création rapide d'un task (rétrocompat, titre suffit). done : l'exception «
  outcome requis pour quick » retirée → aucune tâche ne bloque sur l'outcome (verification
  encouragée mais non bloquante partout — juste retiré la distinction, rien rendu obligatoire). UI :
  zone Mini du Backlog retirée (ex-quick dans To-do trié par température). Skill+guide : quick
  retiré, échelle de décision = type + size. Réalisé par opus, révisé + mergé live (pas de worktree
  : aucun déplacement de dossier).
verification: >-
  tsc 0, 429 tests verts (adaptés : validate rejette quick, quick CLI crée task, done sans outcome
  ne bloque plus). validate OK (234 tâches, 0 quick actif). Dashboard réel : plus de zone Mini
  (liste To-do unique triée température) ; `quick "titre"` en CLI → task (kind absent). Nettoyé les
  ids de test (nextId=252).
release: pre-release
