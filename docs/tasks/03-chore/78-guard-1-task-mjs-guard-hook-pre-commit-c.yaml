id: 100
code: null
title: Guard 1 — task.mjs guard + hook pre-commit committé + activation npm prepare
status: done
tags:
  - process
  - enforcement
size: M
detail: >-
  Nouvelle commande CLI `guard` (scripts/task.mjs) : exit 0 si rien de stagé, OU tous les fichiers
  stagés sous docs/tasks/, OU .git/MERGE_HEAD présent, OU ≥1 tâche in_progress (readTree +
  activeTasks, pas de re-parse maison ; _meta.yaml absent → exit 0, repo non initialisé). Sinon exit
  1, message autoportant : fichiers hors consignation + la commande `node scripts/task.mjs quick
  "<titre>" --team <t> --start` + mention de l'échappatoire --no-verify. Rapide (<300ms, pas de
  validation complète). Hook committé scripts/githooks/pre-commit (exécutable, `exec node
  scripts/task.mjs guard`) + script npm prepare `git config core.hooksPath scripts/githooks || true`
  + activation immédiate dans CE repo. TDD sandbox (pattern task.test.mjs) : bloqué sans in_progress
  ; passe avec in_progress ; passe consignation seule ; passe merge ; message contient quick. Fini
  quand : un VRAI commit sans ticket est refusé dans ce repo, et la consignation passe.
refs:
  - docs/specs/2026-07-08-process-enforcement.md
  - scripts/task.mjs
  - docs/process-enforcement-gap.md
links: []
dependsOn: []
epic: process-enforcement
source: user
createdAt: "2026-07-08T01:36:52"
completedAt: "2026-07-08"
commit: 0e6458e
outcome: >-
  Commande task.mjs guard (exit 1 si fichiers produit stagés sans tâche in_progress ; consignation
  docs/tasks/, merge et repo non initialisé exemptés ; message avec la commande quick exacte +
  --no-verify) ; hook committé scripts/githooks/pre-commit ; activation auto npm prepare →
  core.hooksPath, activée dans ce repo
verification: >-
  7 tests TDD rouges puis verts (sandbox git réel : blocage, in_progress, consignation, merge,
  non-initialisé, e2e vrais commits refusé/accepté via le hook) ; 222 tests + build verts ; hook
  observé tirer sur le VRAI commit de livraison (GIT_TRACE : start_command
  scripts/githooks/pre-commit) et laisser passer grâce au ticket in_progress
release: null
