id: 50
code: null
title: Backlog v2 — liste plate en deux colonnes (ouverts / terminés)
status: done
tags:
  - ux
  - design
size: L
detail: >-
  Batch design Rémi (2026-07-07) — refonte de la vue Backlog : PLUS d'accordéons par stage (les
  stages vivent dans la Roadmap). Liste plate en DEUX colonnes : gauche = tickets ouverts (todo +
  in_progress) du plus ancien créé au plus récent (haut→bas) ; droite = tickets terminés du plus
  récemment bouclé (completedAt) au plus ancien. Tri par createdAt (ids croissants = proxy fiable).
  Header de filtres : recherche texte (titre), filtre team (multi), filtre stage — à affiner sur
  capture avec Rémi. Conserver : lignes TaskRow (ghost sélection accent, chips, sous-tâches ?), « +
  tâche » (repensé : le bouton doit choisir le stage — probablement dans le header). L'état vide et
  l'archive : à re-proposer dans la nouvelle grille. Fini quand : les deux colonnes trient
  correctement (vérifié sur données réelles), filtres fonctionnels, captures relues par Rémi.
refs:
  - src/components/Backlog.tsx
  - src/components/TaskRow.tsx
links: []
dependsOn:
  - 48
epic: vues-dashboard
source: user
createdAt: "2026-07-07"
completedAt: "2026-07-07"
commit: 8211b24
outcome: >-
  Le Backlog est devenu la vue « travail » : deux colonnes plates (ouvertes par ancienneté,
  terminées par date de bouclage), recherche, filtre stage relié à la sidebar, création en un clic
verification: >-
  Capture relue : ouvertes #3→#52 dans l'ordre des ids, terminées #49→#23 dans l'ordre completedAt
  puis id desc, header complet ; recherche « spec » filtre la colonne ouverte (asserté) ; clic stage
  sidebar = filtre surligné accent + Select re-clé ; archive conservée en bas. 128 tests + build
  verts.
release: null
