id: 111
code: null
title: "design.md — le design system léger : tokens, composants BaseUI canoniques, règles"
status: done
tags:
  - design
  - docs
size: M
detail: >-
  Créer docs/design.md — un Design.md, PAS un design system complet (décision : YAGNI pour un
  dashboard local ; le doc peut devenir un système si l'app grossit). Contenu, nourri par les 3
  audits (#107-109) : (1) tokens — palette réduite (avec les corrections de contraste #108), UN fond
  de page canonique (trancher blanc vs gris d'après #109), échelle d'arrondis, échelle d'espacements
  ; (2) composants canoniques — pour chaque besoin (dropdown/select, combobox+tags avec croix, text
  field, dialog, tooltip) LE composant BaseUI retenu et son emplacement dans src/components/ui.tsx,
  interdiction des variantes en dur ; (3) règles — pattern ghost input (inputs permanents camouflés,
  JAMAIS de swap lecture→input ni d'étape crayon — décision Rémi actée), focus visible partout,
  navigation clavier. Chaque règle cite les tâches sources (#107-109). Fini quand : doc relu, chaque
  token/composant tranché (zéro « à décider »), et les 4 tâches d'exécution peuvent s'y référer
  comme source de vérité.
refs:
  - docs/audit-ux-2026-07.md
  - src/components/ui.tsx
  - docs/tone-of-voice.md
links: []
dependsOn:
  - 107
  - 108
  - 109
epic: design-system
source: user
createdAt: "2026-07-08T01:57:01"
completedAt: "2026-07-08"
commit: b08d5ec
outcome: >-
  docs/design.md créé : tokens (accent + tri-couche + échelle de gris avec plancher neutral-500/600,
  règle des deux arrondis, gabarits d'espacement), 9 composants canoniques Base UI mappés sur ui.tsx
  (bordure de champ : option bg-neutral-50 retenue plutôt que border-500), 6 règles (tri-couche
  stricte, langage actif universel accent-tint+filet, pattern ghost acté, focus replacé après
  action, clavier, monochrome sans registre destructif — YAGNI). Zéro « à décider »
verification: >-
  Relu contre la synthèse de l'audit : chacun des 6 points de la synthèse a sa décision dans le doc
  ; chaque règle cite sa source (#36, #107-109) ; les 4 tâches d'exécution (#113-116) peuvent le
  référencer tel quel
release: null
