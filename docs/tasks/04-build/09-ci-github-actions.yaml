id: 9
code: null
title: CI GitHub Actions
status: done
tags:
  - open-source
  - ci
size: S
team: engineering
detail: >-
  Créer .github/workflows/ci.yml : déclenché sur push et pull_request, Node 22, npm ci, puis npm run
  test (vitest) et npm run build (tsc -b + vite build, couvre le typecheck). Ajouter le badge CI
  dans le README. FAIT : ci.yml écrit et validé syntaxiquement (js-yaml), test+build verts en local
  — badge non ajouté (README réécrit en #11, slug GitHub inconnu). RESTE : vérifier le run vert sur
  un push réel — bloqué par l'absence de remote, à finir avec la publication du repo (#13).
refs:
  - package.json
  - vite.config.ts
links: []
dependsOn: []
epic: lancement
source: ai
createdAt: "2026-07-07"
completedAt: "2026-07-08"
commit: a33ffac
outcome: >-
  .github/workflows/ci.yml : sur push et pull_request vers main, job ubuntu-latest — checkout,
  setup-node 22 + cache npm, npm ci, typecheck (npx tsc --noEmit), tests (npx vitest run), build
  (npm run build). Node maintenu à 22 (LTS existante, pas de régression). package.json inchangé.
verification: >-
  Étapes du workflow vérifiées contre les scripts réels de package.json (build = tsc -b && vite
  build existe). tsc --noEmit et vitest run passent en local (233 tests). Non exécuté sur GitHub
  (repo pas encore public, #13) — s'activera au premier push.
release: null
